name: "CodeQL Config"

extraction:
  csharp:
    prepare:
      - command: dotnet restore CodeQLDemo.sln
    index:
      build_command: dotnet build CodeQLDemo.sln
      buildless: false
      solution: CodeQLDemo.sln 